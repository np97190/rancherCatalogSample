apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hospitalmgmt.fullname" . }}-test-connection"
  labels:
{{ include "hospitalmgmt.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hospitalmgmt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
